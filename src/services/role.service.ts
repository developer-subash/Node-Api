class RoleService {

    fetchAll = () => {
        try {
       return 'hello world';

        } catch (error) {

        }
    }
} 

const roleServiceInstance = new RoleService();
export default roleServiceInstance;